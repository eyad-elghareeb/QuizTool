# Generator Updates

This file documented changes to the deprecated `generate_project.py` (Python Flask generator).

The Python generator was removed in V5. Use the Tauri generator instead:
```bash
cd tauri && cargo tauri dev
```
